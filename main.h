#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stdio.h>
#include <unistd.h>

int _putchar(char c);
int _puts(const char *s);
int _printf(const char *format, ...);
int handle_d (int n);
int handle_i (int n);

#endif
